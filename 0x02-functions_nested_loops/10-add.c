#include <stdio.h>

/**
  * add - add two values
  * @a: values to add
  * @b: second value to dd
  * Return: sum of values
  */

int add(int a, int b)
{
	int result;

	result = a + b;

	return (result);
}
